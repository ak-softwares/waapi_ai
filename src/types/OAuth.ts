export enum GoogleService {
  CALENDAR = "google_calendar",
  CONTACTS = "google_contacts",
  SPREADSHEETS = "google_sheets",
  CONTACTS_IMPORT = "google_contacts_import",
}

export enum AuthType {
  CREDENTIALS = "credentials",
  OAUTH = "oauth",
}